{
    "boot_command"                          : "<esc><wait>install <wait>auto <wait>url=%s/preseed.cfg <wait>debian-installer=en_US <wait>locale=en_US <wait>keymap=us <wait>netcfg/get_domain=vm <wait>netcfg/get_hostname=localhost <wait><enter>",
    "floppy_files"                          : "",
    "floppy_dirs"                           : "",
    "http_directory"                        : "../common/files",
    "inject_http_seed_in_boot_command"      : "true"
}
