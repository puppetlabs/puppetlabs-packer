{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "pe_url": "'https://pm.puppetlabs.com/cgi-bin/download.cgi?ver=latest&dist=el&arch=x86_64&rel=7'",
    "provisioner": "ec2",
    "source_ami": "ami-c7d092f7",
    "ami_description": "puppetlabs-centos-7.0-x86_64 {{timestamp}}",
    "ami_name": "puppetlabs-centos-7.0-x86_64 {{timestamp}}",
    "required_modules": "puppetlabs-stdlib saz-ssh",
    "user_data": "#cloud-config\ndisable_root: 0\n"
  },

  "builders": [
    {
      "name": "amazon-ebs-us-west-2",
      "access_key": "{{user `aws_access_key`}}",
      "secret_key": "{{user `aws_secret_key`}}",
      "type": "amazon-ebs",
      "region": "us-west-2",
      "instance_type": "c3.large",
      "ssh_username": "root",
      "ami_description": "{{user `ami_description`}}",
      "ami_name": "{{user `ami_name`}}",
      "source_ami": "{{user `source_ami`}}",
      "region": "us-west-2",
      "ssh_timeout": "10000s",
      "user_data": "{{user `user_data`}}",
      "run_tags": {
        "created_by": "puppetlabs-packer",
        "department": "ESO",
        "project": "puppetlabs-packer"
      },
      "tags": {
        "created_by": "puppetlabs-packer",
        "department": "ESO",
        "project": "puppetlabs-packer"
      }
    }
  ],

  "provisioners": [
    {
      "type": "shell",
      "execute_command": "chmod +x {{ .Path }}; {{ .Vars }} {{ .Path }} {{user `required_modules`}}",
      "environment_vars": [
        "PE_URL={{user `pe_url`}}"
      ],
      "scripts": [
        "../../scripts/bootstrap-puppet.sh"
      ]
    },
    {
      "type": "puppet-masterless",
      "execute_command": "{{.FacterVars}} PATH=$PATH:/opt/puppet/bin /opt/puppet/bin/puppet apply --verbose --detailed-exitcodes --modulepath='/tmp/packer-puppet-masterless/manifests/modules' --manifestdir='{{.ManifestDir}}' {{.ManifestFile}}",
      "facter": {
        "provisioner": "{{user `provisioner`}}"
      },
      "manifest_dir": "../../manifests",
      "manifest_file": "../../manifests/base.pp"
    },
    {
      "type": "shell",
      "scripts": [
        "../../scripts/cleanup-puppet.sh",
        "../../scripts/cleanup-packer.sh",
        "../../scripts/cleanup-ec2.sh"
      ]
    }
  ]
}
